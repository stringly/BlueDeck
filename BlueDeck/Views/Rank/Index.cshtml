@model IEnumerable<BlueDeck.Models.Enums.Rank>

<div style="padding-top:10px; padding-bottom:10px">
    <a asp-action="Create" asp-controller="Race" asp-route-returnUrl="/Rank/Index"><i class="ion-md-person-add"></i> Create a New Rank</a>
</div>
<div>
    @if (ViewBag.Status == "Success!")
    {
        <div class="indexAlertWrapper">
            <div class="alert alert-success alert-dismissible fade in">
                <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                <strong>Success!</strong>
                <span>@ViewBag.Message</span>
            </div>
        </div>

    }
    @if (Model.Count() > 0)
    {
        <div class="table-responsive">
            <table class="table table-condensed">
                <thead>
                    <tr>
                        <th>
                            Full Name
                        </th>
                        <th style="text-align:center">
                            RaceId
                        </th>
                        <th>
                            Short
                        </th>
                        <th>
                            Pay Grade
                        </th>
                        <th>
                            Sworn
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @item.RankFullName
                            </td>
                            <td style="text-align:center">
                                @item.RankId
                            </td>
                            <td style="text-align:center">
                                @item.RankShort
                            </td>
                            <td>
                                @item.PayGrade
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.IsSworn)
                            </td>
                            <td style="text-align:center">
                                <a asp-action="Details" asp-controller="Race" asp-route-id="@item.RankId" asp-route-returnUrl="/Rank/Index" title="View Details" class="glyphicon-ring">
                                    <span class="glyphicon glyphicon-list-alt glyphicon-bordered"></span>
                                </a>
                                <a asp-action="Edit" asp-controller="Race" asp-route-id="@item.RankId" asp-route-returnUrl="/Rank/Index" title="Edit Race" class="glyphicon-ring">
                                    <span class="glyphicon glyphicon-pencil glyphicon-bordered"></span>
                                </a>
                                <a asp-action="Delete" asp-controller="Race" asp-route-id="@item.RankId" asp-route-returnUrl="/Rank/Index" title="Delete Race" class="glyphicon-ring">
                                    <span class="glyphicon glyphicon-trash glyphicon-bordered"></span>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

    }
</div>

<script>
    // close any alert after 3 seconds
    window.setTimeout(function () {
        $(".alert").fadeTo(500, 0).slideUp(500, function () {
            $(this).remove();
        });
    }, 3000);
</script>
